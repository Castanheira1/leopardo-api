<?xml version="1.0" encoding="utf-8" ?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <application
        android:allowBackup="false"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|locale|smallestScreenSize|screenLayout|uiMode|navigation|density"
            android:name=".MainActivity"
            android:label="@string/title_activity_main"
            android:theme="@style/AppTheme.NoActionBarLaunch"
            android:launchMode="singleTask"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
            <!-- App Links: verificação em /.well-known/assetlinks.json (SHA-256 do keystore). -->
            <intent-filter android:autoVerify="true">
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:scheme="https" android:host="leopardo-api.onrender.com" />
            </intent-filter>
        </activity>

        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data android:name="android.support.FILE_PROVIDER_PATHS" android:resource="@xml/file_paths" />
        </provider>

        <!-- Mantém GPS/JS vivos com tela apagada durante a carona (só nativo). -->
        <service
            android:name=".TripTrackingService"
            android:exported="false"
            android:foregroundServiceType="location" />
    </application>

    <!-- Permissions -->

    <uses-permission android:name="android.permission.INTERNET" />
    <!-- Sem VIBRATE o navigator.vibrate() do WebView falha em silêncio. -->
    <uses-permission android:name="android.permission.VIBRATE" />
    <!-- Câmera: selfie ao vivo e foto do veículo (getUserMedia no WebView). -->
    <uses-permission android:name="android.permission.CAMERA" />
    <!-- Localização (uso em primeiro plano): mapa, match por proximidade e rota. -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <!--
      Rastreamento contínuo da viagem via Foreground Service do tipo "location":
      enquanto o serviço roda, o GPS continua liberado com o app minimizado e a tela
      apagada — que é exatamente o caso de uso da carona.

      Não declaramos ACCESS_BACKGROUND_LOCATION de propósito: o serviço só é iniciado
      com o app em primeiro plano, então a permissão seria redundante, e declará-la
      obriga a passar pela revisão de localização em segundo plano do Google Play
      (formulário dedicado + vídeo de demonstração), que atrasa a publicação.
    -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_LOCATION" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-feature android:name="android.hardware.camera" android:required="false" />
    <uses-feature android:name="android.hardware.location.gps" android:required="false" />

    <!--
      Android 11+ filtra a visibilidade de outros apps. Sem isto, checar se o WhatsApp
      está instalado (esquema whatsapp://) falha e o contato cai sempre no wa.me.
    -->
    <queries>
        <package android:name="com.whatsapp" />
        <package android:name="com.whatsapp.w4b" />
        <intent>
            <action android:name="android.intent.action.VIEW" />
            <data android:scheme="https" />
        </intent>
        <intent>
            <action android:name="android.intent.action.DIAL" />
            <data android:scheme="tel" />
        </intent>
    </queries>
</manifest>
